# CLAUDE.md

## Commands

```bash
pnpm install              # install deps + build agentkit submodule
pnpm -s configure         # interactive first-time setup wizard
pnpm -s start             # run agent (requires .env + config.json)
pnpm test                 # vitest run (12 files, ~120 tests)
pnpm test:watch           # vitest watch mode
```

## Git Submodule

- agentkit lives at vendor/agentkit (bob-collective fork, branch feat/bob-gateway-action-provider)
- `git clone --recursive` or `git submodule update --init`
- postinstall script builds it automatically (scripts/build-agentkit.sh)
- NEVER modify files in vendor/agentkit — it's an external dependency

## Architecture

```
src/
  index.ts            # entry point: init + scheduler loop (~100 LOC)
  agent.ts            # LangGraph agents: light (Haiku 4.5) + heavy (Sonnet 4.6) with agentkit
                      # Action providers: wallet, erc20, weth, pyth, vaultsfyi, bobGateway, enso
  config.ts           # merged: env validation (getEnv), path constants, JSON helpers, config schema
  notify.ts           # file logging (data/agent.log) + Telegram alerts: createLogger(tag), notify(level, msg)
  cycle.ts            # 6-step agent cycle + Telegram delta notifications
  prompts.ts          # system prompt fragments: vault selection + rebalance decision
  setup.ts            # interactive CLI setup wizard (standalone CLI script)
  export.ts           # transaction log CSV/JSON export (standalone CLI script)
  modules/
    circle-paymaster-wallet.ts  # ERC-4337 smart account + Circle Paymaster on Base
    instrumented-wallet.ts      # wraps EvmWalletProvider to capture every sendTransaction
    transactions.ts   # TxLogger + receipt-based tx processing (on-chain Transfer event decoding)
    portfolio.ts      # merged: DeFiLlama yields + profit tracking + portfolio value query
    protocol-registry.ts  # persistent vault withdrawal-type cache (instant vs complex)
    telegram.ts       # Telegram bot: commands + inline keyboard (alerts via notify.ts)
    funding-monitor.ts    # watches for incoming USDC transfers
```

## Code Conventions

- All env access through `getEnv()` from src/config.ts — never raw `process.env`
- All logging through `createLogger(tag)` from src/notify.ts — writes to data/agent.log, never console
  (exception: CLI scripts like setup.ts and export.ts use console.log for user output)
- Unified alerts: use `notify(level, msg)` from src/notify.ts — logs + sends Telegram for warn/error
- Error messages: use `safeErrorMessage(err)` from src/notify.ts to avoid leaking stack traces
- JSON file I/O: use `readJsonFile`/`writeJsonFile` from src/config.ts
- Path constants: import from src/config.ts (DATA_DIR, TX_LOG_PATH, LOG_PATH, etc.)

## Testing

- Framework: vitest 4.x
- Tests in `src/__tests__/*.test.ts`
- Heavy deps (viem, permissionless) are mocked with `vi.mock()`
- Circle paymaster tests mock viem, viem/accounts, viem/account-abstraction, permissionless/accounts
- vitest.config.ts excludes `vendor/**` from test discovery
- Run single test: `pnpm exec vitest run src/__tests__/config.test.ts`

## Gotchas

- agentkit must build with TypeScript 5.8.x and opensea-js 7.1.x (pinned in build script) — TS 5.9+ breaks opensea Chain types, opensea-js 7.4+ removes chain names
- pnpm v10 auto-discovers nested pnpm-workspace.yaml — build script removes it from vendor/
- agentkit-langchain has `workspace:*` in devDeps — build script patches it out temporarily
- `@coinbase/agentkit-langchain` depends on `@langchain/core ^0.3.19` but we use 1.x — peer dep warning is expected
- LangChain 1.x: use `prompt` not `messageModifier` in createReactAgent
- dotenv 17.x: use `dotenv.config({ quiet: true })` to suppress missing .env warnings
- BOB Gateway offramp requires native ETH (~0.0005 ETH) for bridge fees — the agent swaps ~$1 USDC→ETH via Enso before calling swap_to_btc
- The smart account uses Circle Paymaster (gas paid in USDC), so ETH is only needed for BOB Gateway value transfers
- Never commit unless explicitly asked
- pnpm only — enforced via preinstall hook

## Environment (.env)

```
ANTHROPIC_API_KEY=     # required
VAULTSFYI_API_KEY=     # optional but needed for vault operations
PRIVATE_KEY=           # auto-generated by setup, hex with 0x prefix
NETWORK_ID=            # default: base-mainnet
TELEGRAM_BOT_TOKEN=    # optional
TELEGRAM_CHAT_ID=      # optional
RPC_URL=               # optional, override default RPC
```
